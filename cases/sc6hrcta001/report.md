# 角雷达问题诊断报告

| 项目 | 内容 |
|------|------|
| 生成时间 | 2026-05-20 16:27:21 |
| 任务类型 | **diagnose** |
| 涉及功能 | **RCTA** |
| 问题现象 | RCTA没有触发 |
| 预期结果 | 应该触发 |
| 分析方法 | 窗口检测 + 条件提取 + TPE + 5专家面板×3轮 |
| BLF数据 | rcta_powermode.blf (55.4s, 36561条) |

---

**根因**
**数据完整性缺失导致根因无法唯一确定**：BLF 文件中缺失所有关键外部抑制信号（GearPos, CarSpd, RCTASwtReq 等），导致 TPE 无法解析 `adasFunc.c` 中的状态机逻辑，且 Variable Probe 显示无满足 `TTM <= 4.2s` 的目标（来源：Variable Probe/抑制信号实测）。最可能的因果链为：**关键 CAN 信号缺失/未录制** → **ECU 无法确认 RCTA 激活条件（Gear=7, Spd<15km/h）** → **系统处于 Passive/Off 状态** → **抑制目标检测与报警逻辑** → **RCTA 未触发**。若假设信号存在但工况不满足，则根因为**车辆未挂倒挡或车速过高**导致系统未激活。

### 时序耦合(TPE触发清单)
| 模式 | 源文件:行 | 首触发t | 持续 | 触发信号 | 副作用 |
|------|----------|--------|------|---------|--------|
| 无 | 无 | 无 | 无 | 无 | 无 |
*(注：TPE 分析显示 8 个相关模式均为 `verdict=无法判定`，因无法解析内部变量到 CAN 信号，无 `triggered` 模式)*

### 条件检查汇总
| 条件 | 阈值 | 实际值 | 满足? | 数据来源 | 相关 TPE 模式 |
|------|------|--------|-------|----------|--------------|
| **系统激活状态** | `rctaSystemState` == 3 (Active) | **无法确认** (信号缺失) | **N/A** | 抑制信号实测/TPE | HoldRelease @ adasFunc.c:4485 |
| **档位 (Gear)** | `actual_gear` == 7 (Reverse) | **未在BLF中找到** | **N/A** | 抑制信号实测 | N/A |
| **自车速度** | `0.0 <= Spd <= 15.0` km/h | **未在BLF中找到** | **N/A** | 抑制信号实测/Variable Probe | N/A |
| **功能使能** | `bRCTAEnable` == TRUE | **未在BLF中找到** | **N/A** | 抑制信号实测 | N/A |
| **目标 TTM** | `TTM <= 4.2` s | **0 行数据** (无目标满足) | **N** | Variable Probe (`ttc`) | N/A |
| **目标 DDCI** | DDCI 在 ROI 内 | **0 行数据** | **N** | Variable Probe (`ddci`) | N/A |

### 关键证据链(结构化)
1. **信号**: `GearPos` (CAN) | **时间**: 全程 | **值**: **缺失** | **来源**: 抑制信号实测 | **TPE 模式**: N/A
   *说明*: 缺少档位信号，无法判断是否满足 RCTA 激活的必要条件（倒挡）。
2. **信号**: `CarSpd` (CAN) | **时间**: 全程 | **值**: **缺失** | **来源**: 抑制信号实测/Variable Probe | **TPE 模式**: N/A
   *说明*: 缺少车速信号，无法判断是否在 0-15 km/h 激活范围内。
3. **信号**: `ttc` (内部变量) | **时间**: 全程 | **值**: **0 行满足 <= 4.2s** | **来源**: Variable Probe | **TPE 模式**: N/A
   *说明*: 无目标满足 TTC 警告阈值，可能是系统未激活导致未计算，或确实无危险目标。
4. **信号**: `bRctaLeftWarningFlg` (内部变量) | **时间**: 全程 | **值**: **无法解析** | **来源**: TPE 分析 | **TPE 模式**: HoldRelease @ adasFunc.c:4485
   *说明*: TPE 无法追踪该标志位的时序，因缺乏底层 CAN 信号支撑。

### 数据链路
CAN信号(`GearPos`, `CarSpd`, `RCTASwtReq`) → **缺失/未录制** → 内部变量(`g_egoCarAddInfo.actual_gear`, `adasEnable.bRCTAEnable`) → **值未知/默认0** → 状态机判断(`ASWIN_SystemState.c`) → **系统状态保持 Passive/Off** → `adasFunc.c` 目标处理逻辑被跳过 → `bRctaLeft/RightWarningFlg` 保持 False → RCTA 未触发

### 测试窗口分析
由于未检测到有效的测试窗口且关键信号缺失，无法进行时序分析。Variable Probe 显示在整个数据段内，没有任何目标被标记为满足 RCTA 警告条件（TTM <= 4.2s），这通常意味着系统未进入 Active 状态，或者雷达未检测到任何后方交叉目标。

### 场景差异分析
若此问题出现在其他正常数据案例中，通常根因是“目标 TTC 大于阈值”或“偏航角超出范围”。但在本案例中，由于**所有外部抑制信号均缺失**，问题性质从“算法逻辑判断”转变为“数据完整性/测试配置问题”。无法区分是“工况不对（没挂倒挡）”还是“数据没录（没录 CAN）”。

### 修复建议
1. **检查数据录制配置**：确认 BLF 文件是否包含车身 CAN 总线信号（特别是 `GearPos`, `CarSpd`, `AccPedPos`）。若缺失，需重新录制数据。
2. **验证测试工况**：若数据完整，请确认测试时车辆是否挂入 **R 档** 且车速 **< 15 km/h**，并开启 RCTA 功能开关。
3. **检查 DBC 映射**：若 CAN 信号存在但解析失败，需检查 DBC 文件是否正确映射了 `GearPos` 等信号到 ECU 内部变量。

### 置信度: 60/100
不确定因素：由于关键 CAN 信号完全缺失，无法区分是“系统因工况不满足而未激活”还是“数据录制配置错误导致信号丢失”。若假设数据完整，则根因置信度提升至 90%（未挂倒挡）；若数据缺失，则无法进行算法层根因分析。