## 问题
{case_context}

## 全部专家分析
{all_opinions}

## 审查结果
矛盾: {contradictions}
遗漏: {gaps}

---

综合诊断，严格遵循以下格式(每节控制在3-5行以内):

**数据溯源规则**: 每个结论的"实际值"和"来源"必须注明出处:
"TPE 因果对齐"/"抑制信号实测"/"条件检查表"/"帧分析数据"/"BAG数据"/"权威阈值参考"/"关键链路信号审计"。
**禁止**引用系统未提供的信号名或自行推断的数据值。如果"抑制信号实测"中标注为"抑制条件不满足"，则不得在结论中将该条件列为根因。
**阈值必须**与"权威阈值参考"(来自source_docs)中的值一致，禁止使用"预估"阈值。
**TPE 规则**: 如果 TPE 段中有 `verdict=triggered` 的模式与本问题相关(触发信号/副作用出现在现象中)，
则根因**必须**以该模式为核心描述；忽略 TPE 触发证据的诊断视为不完整。
**TPE 文件与行号锁定（硬约束）**:
- 最终"根因"段落中提到的代码位置，**必须**使用 TPE 段里给出的
  `file:line_start~line_end`。不允许改写。
- 如果 TPE 段中含多个 triggered 模式，应全部在"时序耦合"表格中呈现。
- 若你认为 TPE 定位有误，必须先写明"TPE 定位: <文件:行>"，再单独开一节说明理由。

**关键链路信号审计(硬约束)**: 若上下文中存在"关键链路信号审计"段，必须先阅读它。
- 审计段标注为"异常"的信号(如持续 Reserved/Invalid 枚举)必须如实呈现在"关键链路信号审计"
  章节中，且应作为排查方向之一。
- 审计段给出的"契约判定"(如"旧 UI 不回传 FCTA_FCTB_Status_S，不构成雷达未响应证据")
  是证据链知识，**不得**与它矛盾；若数据与该契约冲突，写明"违反契约"并说明。
- 缺失的信号(审计段标注"缺席")只能说明"该信号未在录制中出现"，不得臆测其值。

**证据不足规则(硬约束)**: 当同时满足以下条件时，**禁止**给出猜测性根因：
1. TPE 段 `verdict=triggered` 为空（零触发）；
2. 关键链路信号审计没有异常标记；
3. 条件检查表没有 satisfied/violated 的判定（全部无法判定或 N/A）；
4. 帧分析/时间线无任何动态变化。
此时"根因"章节必须写：`无法确定根因（证据不足）`，并把"### 关键证据链(结构化)"
改为列出**CAN 信号事实表**（信号名/观测值/时间点/来源），最后在"置信度"给出不超过
`50/100` 的分数。禁止编造触发时刻、变量值或代码分支作为根因依据。

### 根因
一句话根因 + 因果链: 变量A(实际值X, 来源Y) → 条件B(阈值Z, 满足/不满足) → 结果C
必须显式标明:
- 哪条 TPE 模式触发(模式类型 + 源文件:行号 + 首次触发时刻 + 最短触发持续)
- 该模式的副作用变量如何在数据中反映为现象
(证据不足时按上述规则写"无法确定根因（证据不足）")

### 时序耦合(TPE触发清单)
| 模式 | 源文件:行 | 首触发t | 持续 | 触发信号 | 副作用 |
|------|----------|--------|------|---------|--------|
(摘录所有 `verdict=triggered` 的模式；无触发时明确写"无")

### 条件检查汇总
| 条件 | 阈值 | 实际值 | 满足? | 数据来源 | 相关 TPE 模式 |
(合并所有专家的检查表，去重。"实际值"必须有数据出处，不得编造。)

### 关键链路信号审计
| 信号 | 消息(0xID) | 角色 | 判定 | 观测值 | 异常/备注 |
(摘录审计段的异常标记与契约判定；审计段缺失时写"无")

### 关键证据链(结构化)
每条证据格式: **信号**: `信号名` | **时间**: `t=X.XXXs` | **值**: `实际值` | **来源**: `数据出处` | **TPE 模式**: `模式名/行号`
(列出支撑根因的3-5条关键证据；证据不足时改为 CAN 信号事实表)

### 数据链路
CAN信号→内部变量→判断条件→结果 (一条链路一行)

### 测试窗口分析
窗口内关键数据变化的时序描述(2-3行)

### 场景差异分析
结合功能特性和测试场景，分析现象差异的原因(用数值说明)

### 修复建议
1. xxx
2. xxx
(证据不足时，改为"下一步需要补充的数据/验证"，不要给猜测性修复)

### 置信度: X/100
一句话说明不确定因素；证据不足时 X≤50 且必须列出缺失的证据(如信号缺席、无点火循环窗口)。
